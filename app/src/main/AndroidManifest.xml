<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions - minimal, privacy-first -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_CLIPBOARD" />
    <uses-permission android:name="android.permission.WRITE_CLIPBOARD" />

    <!-- NO INTERNET PERMISSION - Privacy by design -->
    <!-- We explicitly DO NOT request INTERNET permission -->

    <application
        android:name=".LiyoBoardApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiyoBoard"
        tools:targetApi="34">

        <!-- Main Launcher Activity - Setup & Settings -->
        <activity
            android:name=".settings.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".settings.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.LiyoBoard.Onboarding" />

        <!-- Theme Editor Activity -->
        <activity
            android:name=".theme.ThemeEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Theme Preview Activity -->
        <activity
            android:name=".theme.ThemePreviewActivity"
            android:exported="false" />

        <!-- Extension Store Activity -->
        <activity
            android:name=".store.ExtensionStoreActivity"
            android:exported="false" />

        <!-- Font Browser Activity -->
        <activity
            android:name=".fonts.FontBrowserActivity"
            android:exported="false" />

        <!-- Clipboard Manager Activity -->
        <activity
            android:name=".clipboard.ClipboardManagerActivity"
            android:exported="false" />

        <!-- Custom Font Preview Activity -->
        <activity
            android:name=".fonts.FontPreviewActivity"
            android:exported="false" />

        <!-- Keyboard Input Method Service -->
        <service
            android:name=".keyboard.LiyoBoardIME"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Spell Check Service (Future) -->
        <!--
        <service
            android:name=".spellcheck.LiyoBoardSpellChecker"
            android:exported="true"
            android:label="@string/spell_checker_name"
            android:permission="android.permission.BIND_SPELL_CHECKER">
            <intent-filter>
                <action android:name="android.service.textservice.SpellCheckerService" />
            </intent-filter>
            <meta-data
                android:name="android.view.textservice.scs"
                android:resource="@xml/spellchecker" />
        </service>
        -->

        <!-- File Provider for sharing themes and extensions -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
